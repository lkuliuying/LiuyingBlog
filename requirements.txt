# ============================================
# 流萤博客 - Python 依赖包
# 最后更新: 2026-04-25
# ============================================

# 核心框架
Django==6.0.3
asgiref==3.11.1
sqlparse==0.5.5
tzdata==2026.1

# Django REST Framework (API 接口)
djangorestframework==3.15.1

# 后台管理主题
django-jazzmin==3.0.4

# 环境变量管理
python-dotenv==1.0.1

# 异步任务队列
celery==5.4.0

# 数据库驱动（可选，当前使用 SQLite）
# mysqlclient==2.2.8  # MySQL 支持（已注释，如需使用请取消注释）

# Redis 缓存（可选，生产环境推荐）
# django-redis==5.4.0  # Redis 缓存后端（已注释，如需使用请取消注释）
# redis==5.0.4         # Redis Python 客户端（已注释，如需使用请取消注释）

# Markdown 支持（可选，用于富文本处理）
Markdown==3.10.2
pymdown-extensions==10.21.2
PyYAML==6.0.3

# 图片处理（Django ImageField 需要）
Pillow==12.2.0

# ============================================
# 开发工具（可选）
# ============================================
# pytest==8.0.0          # 测试框架
# pytest-django==4.8.0   # Django 测试支持
# black==24.2.0          # 代码格式化
# flake8==7.0.0          # 代码检查
# ============================================
